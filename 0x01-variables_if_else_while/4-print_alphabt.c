#include <stdio.h>

/**
 * main - print all letters except q and e
 * Return: Always 0
 */

int main(void)
{

	char letters;

	for (letters = 'a'; latters <= 'z'; letters++)
	{
		if !((letters = 'q') && (letters = 'e'))
		{
			putchar(letters);
		}
	}
	putchar('\n');
	return(0);
}
